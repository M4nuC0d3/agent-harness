{
  "$comment": [
    "PROJECT-SPECIFIC. The only file in .claude/ that describes your stack.",
    "format.py (PostToolUse) walks these rules in order and runs the first one",
    "whose prefix and extension match the file just written. Best-effort: a",
    "formatter that is not on PATH is skipped, never an error. Empty rules[] is",
    "valid and disables auto-formatting entirely.",
    "prefix matches the repo-relative path; '' matches every file."
  ],
  "rules": [
    {
      "prefix": "example/frontend/",
      "extensions": [".ts", ".html", ".scss", ".css", ".js", ".mjs", ".json"],
      "command": ["npx", "--no-install", "prettier", "--write", "{file}"],
      "requires": "npx"
    },
    {
      "prefix": "example/backend/",
      "extensions": [".java"],
      "command": ["google-java-format", "-i", "{file}"],
      "requires": "google-java-format"
    }
  ]
}
